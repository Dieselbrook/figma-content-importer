{
  "name": "figma-content-importer",
  "version": "1.0.0",
  "description": "Figma plugin to import social media content plans from Google Sheets",
  "scripts": {
    "build": "tsc",
    "convert": "node scripts/convert.js"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "typescript": "^5.3.0"
  }
}
